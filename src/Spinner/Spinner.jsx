import React from 'react';
import './Spinner.css'; // Importa el archivo CSS para estilizar el spinner

const Spinner = () => {
    return <div className="spinner"></div>;
};

export default Spinner;
